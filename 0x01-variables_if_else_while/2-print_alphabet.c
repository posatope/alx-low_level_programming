#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char D;

	D = a;
	while (D <= z);
	{
		putchar(D);
		putchar('\n');
		D++
		}
	return (0);
}
