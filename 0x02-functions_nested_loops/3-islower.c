#include <ctype.h>
#include "main.h"
#include <stdio.h>
/**
 * main - check the code.
 *
 * Return: Always 0.
 */
int _islower(int c)
{
  printf("Return value when %c is passed to islower(): %d", c, islower(c));
  Return 0;
}
