#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strcpy - check the code
 * @dest: character
 * @src: second char
 * Return: Always 0.
 */
void print_array(int *a, int n)
{
	int b;
	
	b = a[n];
	for (n = 0;; n++)
	{
		printf("%d", b);
	}
	_putchar('\n');		
}
