#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strcpy - check the code
 * @dest: character
 * @src: second char
 * Return: Always 0.
 */
void print_array(int *a, int n)
{
	int i,j;
	
	j[i] = a[n];
	for ( i = 0; i <= n; i++)
		printf("%d, ", j[i]);
	_putchar('\n');		
}
