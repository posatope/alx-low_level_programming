#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strcpy - check the code
 * @dest: character
 * @src: second char
 * Return: Always 0.
 */
void print_array(int *a, int n)
{
	printf("%d, ", a[n]);
	n++;
	_putchar('\n');		
}
