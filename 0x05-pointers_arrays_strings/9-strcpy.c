#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strcpy - check the code
 * @dest: character
 * @src: second char
 * Return: Always 0.
 */
char *_strcpy(char *dest, char *src)
{
	strcpy(dest, *src);
}
  
