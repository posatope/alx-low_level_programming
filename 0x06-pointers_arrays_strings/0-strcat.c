#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strcat - check the code
 * @dest: char 1
 * @src: char 2
 * Return: Always 0.
 */
char *_strcat(char *dest, char *src)
{
	strcat(dest, src); 
	puts(str1);
}
