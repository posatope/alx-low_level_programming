#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strncat - check the code
 * @dest: char 1
 * @src: char 2
 * @n: int size
 * Return: Always 0.
 */
char *_strncat(char *dest, char *src, int n)
{
	strncat(dest, src, n); 
	puts(str1);
}
